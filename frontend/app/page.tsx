'use client';

import GlobalLeaderboard from '@/components/GlobalLeaderboard';

const Home = () => {
  return (
    <div className="flex">
      <GlobalLeaderboard />
    </div>
  );
};

export default Home;
